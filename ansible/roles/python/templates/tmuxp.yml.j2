session_name: {{ tmux_session_name }}
windows:
  - window_name: {{ tmux_window_title }}
    panes:
      - shell_command:
        - source {{ virtualenv_dir }}/bin/activate
        - {{ python_script }} -P {{ processes }} {{ per_host_args }} {{ common_args }} > {{ log_file }} 2>&1
